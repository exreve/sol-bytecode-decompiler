[package]
name = "n_token"
version = "0.1.0"
edition = "2021"
rust-version = "1.84"

[lib]
crate-type = ["cdylib", "lib"]

[features]
default = []
v_cpi_unchecked = []
v_recipient_unbound = []
v_no_signer = []


[dependencies]
solana-program = { workspace = true }
spl-token = { workspace = true }
